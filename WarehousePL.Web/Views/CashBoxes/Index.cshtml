@using WarehouseBLL.BusinessServices.View_Models.CashBox
@using WarehouseBLL.Const
@model IEnumerable<CashBoxViewModel>
@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery antiforgery

@{
    ViewBag.Title = _localization["CashBoxes"]; 
}

<div class="card">
    <div class="card-datatable text-nowrap">
        <table class="js-table datatables-basic table table-bordered table-responsive">
            <thead>
                <tr>
                    <th>@_localization[DisplayNames.Name]</th>
                    <th>@_localization[DisplayNames.Branch]</th>
                    <th>@_localization[DisplayNames.CurrentBalance]</th>
                    <th>@_localization[DisplayNames.Status]</th>
                    <th>@_localization[DisplayNames.CashTransactions]</th>
                    <th class="js-no-export" data-i18n="Edit">@_localization[DisplayNames.Edit]</th>
                    <th class="js-no-export" data-i18n="DeleteOrRestore">@_localization[DisplayNames.DeleteOrRestore]</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var cashBox in Model)
                {
                    <partial name="_Row" model="@cashBox" />
                }
            </tbody>
        </table>
    </div>
</div>

<div id="create-button-data"
     data-address="@_localization["CashBoxes"]"
     data-title="@_localization["AddCashBox"]"
     data-url="@Url.Action("Create", "CashBoxes")"
     data-label="@_localization["AddCashBox"]"
     data-mode="modal"></div>

<input type="hidden" id="RequestVerificationToken" name="__RequestVerificationToken" value="@antiforgery.GetAndStoreTokens(Context).RequestToken" />

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}