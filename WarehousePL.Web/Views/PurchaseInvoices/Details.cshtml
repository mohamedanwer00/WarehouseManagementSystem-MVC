@using WarehouseBLL.FormViewModels.PurchaseInvoice
@model PurchaseInvoiceDetailsViewModel

@{
    ViewBag.Title = "تفاصيل فاتورة المشتريات";
}

<div class="card mb-4 shadow-sm border-0">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center py-3">
        <h5 class="mb-0 fw-bold"><i class="bx bx-receipt me-2"></i>تفاصيل فاتورة رقم: @Model.InvoiceNumber</h5>
        <a asp-action="Index" class="btn btn-light btn-sm fw-bold">الرجوع للقائمة</a>
    </div>

    <div class="card-body">
        <div class="row g-3 mb-4">
            <div class="col-md-3">
                <strong>تاريخ الفاتورة:</strong>
                <p class="text-muted mb-0">@Model.InvoiceDate.ToString("yyyy-MM-dd")</p>
            </div>
            <div class="col-md-3">
                <strong>المورد:</strong>
                <p class="text-muted mb-0">@Model.SupplierName</p>
            </div>
            <div class="col-md-3">
                <strong>الفرع:</strong>
                <p class="text-muted mb-0">@Model.BranchName</p>
            </div>
            <div class="col-md-3">
                <strong>المخزن:</strong>
                <p class="text-muted mb-0">@Model.WarehouseName</p>
            </div>
        </div>

        <h5 class="fw-bold mb-3 border-bottom pb-2">أصناف الفاتورة</h5>
        <div class="table-responsive mb-4">
            <table class="table table-bordered text-center align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>#</th>
                        <th>الصنف</th>
                        <th>الوحدة</th>
                        <th>الكمية</th>
                        <th>سعر الشراء</th>
                        <th>الخصم</th>
                        <th>الإجمالي</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Items != null && Model.Items.Any())
                    {
                        int index = 1;
                        foreach (var item in Model.Items)
                        {
                            <tr>
                                <td>@(index++)</td>
                                <td>@item.ProductName</td>
                                <td>@item.UnitName</td>
                                <td>@item.Quantity</td>
                                <td>@item.PurchasePrice.ToString("N2")</td>
                                <td>@(item.Discount?.ToString("N2") ?? "0.00")</td>
                                <td>@item.TotalPrice.ToString("N2")</td>
                            </tr>
                        }    

                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-muted">لا توجد أصناف في هذه الفاتورة.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="row g-3 bg-light p-3 rounded">
            <div class="col-md-3">
                <strong>إجمالي الخصم:</strong>
                <h6 class="text-danger mb-0">@(Model.Discount?.ToString("N2") ?? "0.00")</h6>
            </div>
            <div class="col-md-3">
                <strong>صافي الفاتورة:</strong>
                <h6 class="text-primary mb-0">@Model.TotalAmount.ToString("N2")</h6>
            </div>
            <div class="col-md-3">
                <strong>المدفوع:</strong>
                <h6 class="text-success mb-0">@(Model.Paid?.ToString("N2") ?? "0.00")</h6>
            </div>
            <div class="col-md-3">
                <strong>المتبقي:</strong>
                <h6 class="text-warning mb-0">@(Model.Remaining?.ToString("N2") ?? "0.00")</h6>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(Model.Notes))
        {
            <div class="mt-3">
                <strong>ملاحظات:</strong>
                <p class="text-muted mb-0">@Model.Notes</p>
            </div>
        }
    </div>
</div>

@* @section Scripts {
    <script>
        $(document).ready(function () {
            // فحص ما إذا كان الرابط يحتوي على print=true
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('print') === 'true') {
                // فتح نافذة الطباعة تلقائياً
                window.print();
            }
        });
    </script>
} *@


@*                 @using WarehouseBLL.FormViewModels.PurchaseInvoice
@model PurchaseInvoiceDetailsViewModel

@{
    ViewBag.Title = "تفاصيل فاتورة المشتريات";
}

<div class="card mb-4 shadow-sm border-0">

    <!-- Header -->
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center py-3">
        <h5 class="mb-0 fw-bold">
            <i class="bx bx-receipt me-2"></i>
            فاتورة رقم: @Model.InvoiceNumber
        </h5>
        <a asp-action="Index" class="btn btn-light btn-sm fw-bold">
            <i class="bx bx-arrow-back me-1"></i>
            الرجوع للقائمة
        </a>
    </div>

    <div class="card-body">

        <!-- بيانات الفاتورة الأساسية -->
        <div class="row g-3 mb-4">

            <div class="col-md-3 col-sm-6">
                <div class="p-3 bg-light rounded h-100">
                    <div class="text-muted small mb-1">
                        <i class="bx bx-calendar me-1"></i>
                        تاريخ الفاتورة
                    </div>
                    <div class="fw-bold">
                        @Model.InvoiceDate.ToString("yyyy-MM-dd")
                    </div>
                </div>
            </div>

            <div class="col-md-3 col-sm-6">
                <div class="p-3 bg-light rounded h-100">
                    <div class="text-muted small mb-1">
                        <i class="bx bx-user me-1"></i>
                        المورد
                    </div>
                    <div class="fw-bold">
                        @Model.SupplierName
                    </div>
                </div>
            </div>

            <div class="col-md-3 col-sm-6">
                <div class="p-3 bg-light rounded h-100">
                    <div class="text-muted small mb-1">
                        <i class="bx bx-store me-1"></i>
                        الفرع
                    </div>
                    <div class="fw-bold">
                        @Model.BranchName
                    </div>
                </div>
            </div>

            <div class="col-md-3 col-sm-6">
                <div class="p-3 bg-light rounded h-100">
                    <div class="text-muted small mb-1">
                        <i class="bx bx-buildings me-1"></i>
                        المخزن
                    </div>
                    <div class="fw-bold">
                        @Model.WarehouseName
                    </div>
                </div>
            </div>

        </div>

        <!-- جدول الأصناف -->
        <h5 class="fw-bold mb-3 border-bottom pb-2">
            <i class="bx bx-list-ul me-2"></i>
            أصناف الفاتورة
        </h5>

        <div class="table-responsive mb-4">
            <table class="table table-bordered text-center align-middle mb-0">
                <thead class="table-dark">
                    <tr>
                        <th width="5%">#</th>
                        <th width="25%">الصنف</th>
                        <th width="15%">الوحدة</th>
                        <th width="12%">الكمية</th>
                        <th width="15%">سعر الشراء</th>
                        <th width="13%">الخصم</th>
                        <th width="15%">الإجمالي</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Items != null && Model.Items.Any())
                    {
                        int index = 1;
                        foreach (var item in Model.Items)
                        {
                            <tr>
                                <td>@(index++)</td>
                                <td class="text-start fw-semibold">@item.ProductName</td>
                                <td>
                                    <span class="badge bg-label-secondary">@item.UnitName</span>
                                </td>
                                <td>@item.Quantity</td>
                                <td>@item.PurchasePrice.ToString("N2")</td>
                                <td class="text-danger">
                                    @(item.Discount?.ToString("N2") ?? "0.00")
                                </td>
                                <td class="fw-bold">@item.TotalPrice.ToString("N2")</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-muted py-4">
                                <i class="bx bx-info-circle me-1"></i>
                                لا توجد أصناف في هذه الفاتورة.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- ملخص الحسابات -->
        <div class="row g-3">

            <div class="col-md-9">

                @if (!string.IsNullOrEmpty(Model.Notes))
                {
                    <div class="p-3 bg-light rounded h-100">
                        <div class="text-muted small mb-1">
                            <i class="bx bx-note me-1"></i>
                            ملاحظات
                        </div>
                        <div>@Model.Notes</div>
                    </div>
                }

            </div>

            <div class="col-md-3">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body p-3">

                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span class="text-muted">إجمالي الخصم</span>
                            <span class="fw-bold text-danger">
                                @(Model.Discount?.ToString("N2") ?? "0.00")
                            </span>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span class="text-muted">المدفوع</span>
                            <span class="fw-bold text-success">
                                @(Model.Paid?.ToString("N2") ?? "0.00")
                            </span>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span class="text-muted">المتبقي</span>
                            <span class="fw-bold text-warning">
                                @(Model.Remaining?.ToString("N2") ?? "0.00")
                            </span>
                        </div>

                        <hr class="my-2" />

                        <div class="d-flex justify-content-between align-items-center">
                            <span class="fw-bold">صافي الفاتورة</span>
                            <span class="fw-bold text-primary fs-5">
                                @Model.TotalAmount.ToString("N2")
                            </span>
                        </div>

                    </div>
                </div>
            </div>

        </div>

    </div>
</div>

@* @section Scripts {
    <script>
        $(document).ready(function () {
            // فحص ما إذا كان الرابط يحتوي على print=true
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('print') === 'true') {
                // فتح نافذة الطباعة تلقائياً
                window.print();
            }
        });
    </script>
} *@ 